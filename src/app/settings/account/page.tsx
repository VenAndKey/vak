import AccountClient from "./AccountClient";

export const metadata = {
  title: "Account - Veneer and Keying",
  description: "Manage your account security settings.",
};

export default function AccountPage() {
  return <AccountClient />;
}
